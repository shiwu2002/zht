<!--pages/favorite/favorite.wxml-->
<view class="container">
  <view class="item-list">
    <view class="item-card" wx:for="{{itemList}}" wx:key="id" 
          data-id="{{item.id}}" bindtap="toDetail">
      <image class="item-image" src="{{item.images[0]}}" mode="aspectFill"></image>
      <view class="item-info">
        <text class="item-title">{{item.title}}</text>
        <text class="item-desc">{{item.description}}</text>
        <view class="item-meta">
          <image class="location-icon" src="/images/add/jiaju.png" mode="aspectFit"></image>
          <text class="item-location">{{item.location}}</text>
        </view>
      </view>
      <view class="delete-btn" catchtap="removeFavorite" data-id="{{item.id}}">✕</view>
    </view>
  </view>

  <view class="empty" wx:if="{{itemList.length === 0}}">
    <text>暂无收藏</text>
  </view>

  <view class="load-more" wx:if="{{loading}}">加载中...</view>
</view>
